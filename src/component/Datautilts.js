// dataUtils.js
// const combineData = (incomeData, expenseData, savingData) => {
//     if (!Array.isArray(incomeData)) {
//       console.error('Income data is not an array:', incomeData);
//       return [];
//     }
  
//     // Implement the logic to combine the data here
//     // You may want to merge the arrays based on a common field like 'date'
//     // Return the combined array
//     // For now, let's just return the combined array as is
//     return [...incomeData, ...expenseData, ...savingData];
//   };
  